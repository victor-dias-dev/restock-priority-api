services:
  db:
    image: postgres:16-alpine
    container_name: restock-db
    environment:
      POSTGRES_DB: restock
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d restock"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: .
    container_name: restock-api
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/restock
      PORT: "3000"
    ports:
      - "3000:3000"

volumes:
  pgdata:
