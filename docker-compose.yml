services:
  db:
    image: postgres:16-alpine
    container_name: karhub-db
    environment:
      POSTGRES_DB: karhub
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
